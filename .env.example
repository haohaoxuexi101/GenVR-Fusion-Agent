# Never commit a real key. DeepSeek is optional; the causal policy needs no API.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
# Comma-separated list for model-matrix validation (default: flash and pro).
DEEPSEEK_MODELS=
